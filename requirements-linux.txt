# Зависимости для Linux (с системным ROS)
# Сначала установите ROS через apt:
# sudo apt install ros-noetic-desktop-full
# source /opt/ros/noetic/setup.bash

# Затем установите Python пакеты:
numpy>=1.19.0
scipy>=1.5.0
scikit-learn>=0.24.0
matplotlib>=3.3.0
laspy>=2.0.0
lazrs>=0.4.0

# ROS пакеты берутся из системы, не из pip
